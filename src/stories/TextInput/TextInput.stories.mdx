import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { TextInput } from '@paper/components';

<Meta title="Input/Text Input"
      component={TextInput}
/>


# Text Input
Basic Text Input component usage.

<Canvas>
  <Story
    name="Text Input"
    args={{
      title: "Title example",
      placeholderText: "Text input example",
      disabled: false
    }}
  >
    {(args) => <TextInput {...args}/>}
  </Story>
</Canvas>

<ArgsTable story="Text Input"/>

## Stories


#### Disabled input
<Canvas>
  <Story
    name="Disabled Text Input"
    args={{
      title: "Disabled input example",
      placeholderText: "Disabled input example",
      disabled: true
    }}
  >
    {(args) => <TextInput {...args}/>}
  </Story>
</Canvas>

#### Input with error

<Canvas>
  <Story
    name="Input with error"
    args={{
      title: "Error example",
      placeholderText: "Error input example",
      disabled: false,
      error: "I have error"
    }}
  >
    {(args) => <TextInput {...args}/>}
  </Story>
</Canvas>

