import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import {
  Heading,
  HEADING_TYPE,
  ICON,
  Icon,
  ICONS_COLOR,
  ICONS_SIZE,
  PopoverBlock, PopoverMenu, Text,
  TEXT_TYPE,
} from '@paper/components';

<Meta title="Popover/Popover Menu"
      component={PopoverBlock}
/>


# Popover Menu
Basic Popover Menu" component usage.

<Canvas>
  <Story
    name="Popover Menu"
    args={{
    }}
  >
    {(args) => <PopoverMenu {...args}>
      <PopoverBlock>
        <Icon
          className="p-icon__animated"
          size={ICONS_SIZE.LARGER}
          icon={ICON.SEARCH}
          color={ICONS_COLOR.DARK}
        />
        <Heading type={HEADING_TYPE.SMALL} className="popover-heading">
          From Clippings
        </Heading>
        <Text type={TEXT_TYPE.SMALL} className="graphite-60 text-align-center">
          Add a product from the Clippings catalogue
        </Text>
      </PopoverBlock>
      <PopoverBlock>
        <Icon
          className="p-icon__animated"
          size={ICONS_SIZE.LARGER}
          icon={ICON.UPLOAD}
          color={ICONS_COLOR.DARK}
        />
        <Heading type={HEADING_TYPE.SMALL} className="popover-heading">
          From elsewhere
        </Heading>
        <Text type={TEXT_TYPE.SMALL} className="graphite-60 text-align-center">
          Add a product you’ve found elsewhere
        </Text>
      </PopoverBlock>
      <PopoverBlock>
        <Icon
          className="p-icon__animated"
          size={ICONS_SIZE.LARGER}
          icon={ICON.FOLDER}
          color={ICONS_COLOR.DARK}
        />
        <Heading type={HEADING_TYPE.SMALL} className="popover-heading">
          Create a space
        </Heading>
        <Text type={TEXT_TYPE.SMALL} className="graphite-60 text-align-center">
          Organise products into rooms or areas
        </Text>
      </PopoverBlock>
    </PopoverMenu>}
  </Story>
</Canvas>
