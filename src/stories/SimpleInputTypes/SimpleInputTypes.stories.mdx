import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { PhoneInput, TextInput, UrlInput } from '@paper/components';

<Meta title="Input/Simple Input Types"
  component={TextInput}
/>


# Basic Input Types
Please be advised when you use different types of field they will come with the validation of the specific type. To ignore that validation add `novalidate` attribute to the form

## Text Input
<Canvas>
  <Story
    name="Text Input"
    args={{
      title: "Title example",
      placeholderText: "Text input example",
      disabled: false,
      note: "Some Example Note"
    }}
  >
    {(args) => <TextInput {...args}/>}
  </Story>
</Canvas>

<ArgsTable story="Text Input"/>

## URL Input

<Canvas>
  <Story
    name="URL input"
    args={{
      title: "Url example",
      placeholderText: "https://",
      disabled: false
    }}
  >
    {(args) => <UrlInput {...args}/>}
  </Story>
</Canvas>

<ArgsTable story="URL input"/>

## Email Input

<Canvas>
  <Story
    name="Email input"
    args={{
      title: "Email example",
      placeholderText: "example@clipping.com",
      disabled: false,
      note: "Some Example Note"
    }}
  >
    {(args) => <UrlInput {...args}/>}
  </Story>
</Canvas>

<ArgsTable story="Email input"/>

## Phone Input

<Canvas>
  <Story
    name="Phone input"
    args={{
      title: "Phone example",
      placeholderText: "+000 000 000",
      disabled: false
    }}
  >
    {(args) => <PhoneInput {...args}/>}
  </Story>
</Canvas>

<ArgsTable story="Phone input"/>

