import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { CircleButtonExample }
  from '@paper/enums/stories/Buttons/ButtonsExample';
import { Icon } from '@paper/components';
import { BUTTON_SIZE, BUTTON_VARIANT, ICON, ICONS_COLOR, ICONS_SIZE } from '@paper/enums';

<Meta title="Buttons/Circle" />

export const CircleButton = args => <CircleButtonExample {...args} />;

# Buttons
Basic buttons usage of the `CircleButton`.

<Canvas>
  <Story
    name="Default Circle button"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)
    }
  </Story>
</Canvas>

<ArgsTable story="Default Circle button"/>

## Stories

#### Primary

The Primary variant is only variant what is  going to be affected by the change of the theme

<Canvas>
  <Story
    name="Primary Circle button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
  <Story
    name="Primary Circle button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => (
      <CircleButton {...args} >
        <Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
      </CircleButton>
    )}
  </Story>
  <Story
    name="Primary Circle button small"
    argTypes={{
      theme: {
        control: {
        type: 'select',
        options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
  {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.PLUS} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
</Canvas>

#### Secondary

The secondary variant is **not** going to be affected by the change of the theme

<Canvas>
  <Story
    name="Secondary Circle button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
  args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
  <Story
  name="Secondary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.PLUS} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
  <Story
    name="Secondary Circle button small"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.BELL} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
</Canvas>


#### Tertiary

The tertiary variant is **not** going to be affected by the change of the theme

<Canvas>
    <Story
      name="Tertiary Circle button big"
      argTypes={{
        theme: {
          control: {
            type: 'select',
            options: ['light', 'kazam'],
          },
        }
      }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.ARROW_UP} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
  <Story
    name="Tertiary Circle button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
  <Story
    name="Tertiary Circle button small"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => (<CircleButton {...args} >
      <Icon icon={ICON.PLUS} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />
    </CircleButton>)}
  </Story>
</Canvas>

