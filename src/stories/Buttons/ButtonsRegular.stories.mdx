import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { ButtonRegularExample }
  from '@paper/enums/stories/Buttons/ButtonsExample';
import { BUTTON_SIZE, BUTTON_VARIANT } from '@paper/enums';

<Meta title="Buttons/Regular" />

export const Button = args => <ButtonRegularExample {...args} />;

# Buttons
Basic buttons usage

<Canvas>
  <Story
    name="Default button"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.BIG,
      fullWidth: false
    }}
  >
    {(args) => <Button {...args}>Primary Big</Button>}
  </Story>
</Canvas>

<ArgsTable story="Default button"/>

## Stories

#### Primary

The Primary variant is only variant what is  going to be affected by the change of the theme

<Canvas>
  <Story
    name="Primary button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => <Button {...args}>Primary Big</Button>}
  </Story>
  <Story
    name="Primary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => <Button {...args}>Primary Default</Button>}
  </Story>
  <Story
    name="Primary button small"
    argTypes={{
      theme: {
        control: {
        type: 'select',
        options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
  {(args) => <Button {...args}>Primary Small</Button>}
  </Story>
</Canvas>

#### Secondary

The secondary variant is **not** going to be affected by the change of the theme

<Canvas>
  <Story
    name="Secondary button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
  args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => <Button {...args}>Secondary Big</Button>}
  </Story>
  <Story
  name="Secondary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => <Button {...args}>Secondary Default</Button>}
  </Story>
  <Story
    name="Secondary button small"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => <Button {...args}>Secondary Small</Button>}
  </Story>
</Canvas>

#### Tertiary

The tertiary variant is **not** going to be affected by the change of the theme

<Canvas>
  <Story
    name="Tertiary button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => <Button {...args}>Tertiary Big</Button>}
  </Story>
  <Story
    name="Тertiary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => <Button {...args}>Тertiary Default</Button>}
  </Story>
  <Story
    name="Тertiary button small"
    argTypes={{
      theme: {
        control: {
        type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => <Button {...args}>Тertiary Small</Button>}
  </Story>
</Canvas>
