import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';
import { ButtonRegularExample }
  from '@paper/enums/stories/Buttons/ButtonsExample';
import { Icon } from '@paper/components';
import { BUTTON_SIZE, BUTTON_VARIANT, ICON, ICONS_COLOR, ICONS_SIZE } from '@paper/enums';

<Meta title="Buttons/Regular" />

export const Button = args => <ButtonRegularExample {...args} />;

# Buttons
Basic buttons usage

<Canvas>
  <Story
    name="Default button"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.BIG,
      fullWidth: false,
      disabled: false,
    }}
  >
    {(args) => <Button {...args}>Primary Big</Button>}
  </Story>
</Canvas>

<ArgsTable story="Default button"/>

## Stories

#### Primary

The Primary variant is only variant what is  going to be affected by the change of the theme

<Canvas>
  <Story
    name="Primary button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => <Button
      endIcon={<Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />}
      {...args}>
      Primary big
    </Button>}
  </Story>
  <Story
    name="Primary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => <Button{...args}>Primary default </Button>}
  </Story>
  <Story
    name="Primary button small"
    argTypes={{
      theme: {
        control: {
        type: 'select',
        options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.PRIMARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => <Button {...args}>Primary Small</Button>}
  </Story>
</Canvas>

#### Secondary

The secondary variant is **not** going to be affected by the change of the theme

<Canvas>
  <Story
    name="Secondary button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => <Button
      startIcon={<Icon icon={ICON.PLUS} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />}
      {...args}>
      Secondary big
    </Button>}
  </Story>
  <Story
  name="Secondary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => <Button
      {...args}>
      Secondary Default
    </Button>}
  </Story>
  <Story
    name="Secondary button small"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.SECONDARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => <Button {...args}>Secondary Small</Button>}
  </Story>
</Canvas>

#### Tertiary

The tertiary variant is **not** going to be affected by the change of the theme

<Canvas>
  <Story
    name="Tertiary button big"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.BIG
    }}
  >
    {(args) => <Button
      endIcon={<Icon icon={ICON.ARROW_UP} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />}
      {...args}>
      Tertiary Big</Button>
    }
  </Story>
  <Story
    name="Тertiary button default"
    argTypes={{
      theme: {
        control: {
          type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.DEFAULT
    }}
  >
    {(args) => <Button {...args}>Tertiary Default</Button>}
  </Story>
  <Story
    name="Тertiary button small"
    argTypes={{
      theme: {
        control: {
        type: 'select',
          options: ['light', 'kazam'],
        },
      }
    }}
    args={{
      theme: 'light',
      variant: BUTTON_VARIANT.TERTIARY,
      size: BUTTON_SIZE.SMALL
    }}
  >
    {(args) => <Button {...args}>Тertiary Small</Button>}
  </Story>
</Canvas>


#### Text
  <Canvas>
    <Story
      name="Text button big"
      argTypes={{
        theme: {
        control: {
            type: 'select',
            options: ['light', 'kazam'],
          },
        }
      }}
    >
      {(args) => <Button {...args}>Only Text</Button>}
    </Story>
    <Story
      name="Text button icon start"
      argTypes={{
        theme: {
          control: {
            type: 'select',
            options: ['light', 'kazam'],
          },
        }
      }}
    >
      {(args) => <Button startIcon={<Icon icon={ICON.PLUS} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />} {...args}>Text with icon</Button>}
    </Story>
    <Story
      name="Text button icon end"
      argTypes={{
        theme: {
          control: {
            type: 'select',
            options: ['light', 'kazam'],
          },
        }
      }}
    >
      {(args) => <Button
        endIcon={<Icon icon={ICON.BOARD} color={ICONS_COLOR.DARK} size={ICONS_SIZE.SMALL} />}
        {...args}>
        Tex and icon</Button>
      }
    </Story>
  </Canvas>

